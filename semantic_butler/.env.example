# Semantic Desktop Butler - Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# OPENROUTER CONFIGURATION (Primary AI Provider)
# ===========================================
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=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

# Optional: Site info for OpenRouter dashboard analytics
OPENROUTER_SITE_URL=https://semantic-butler.app
OPENROUTER_SITE_NAME=Semantic Desktop Butler

# ===========================================
# MODEL PREFERENCES (Optional Overrides)
# ===========================================
# Override default model selections
# See available models: https://openrouter.ai/models

# Embedding model (default: openai/text-embedding-3-small)
# EMBEDDING_MODEL=openai/text-embedding-3-small

# Fast chat model for simple tasks (default: google/gemini-flash-1.5)
# CHAT_MODEL_FAST=google/gemini-flash-1.5

# Quality chat model for complex tasks (default: anthropic/claude-3.5-sonnet)
# CHAT_MODEL_QUALITY=anthropic/claude-3.5-sonnet

# Agent model for multi-step reasoning (default: anthropic/claude-3.5-sonnet)
# AGENT_MODEL=anthropic/claude-3.5-sonnet

# ===========================================
# FALLBACK API KEYS (Optional)
# ===========================================
# Direct API keys for fallback if OpenRouter is unavailable

# Google Gemini (optional fallback for embeddings)
# GEMINI_API_KEY=your-gemini-key

# OpenAI (optional fallback)
# OPENAI_API_KEY=your-openai-key

# Anthropic (optional fallback)
# ANTHROPIC_API_KEY=your-anthropic-key

# ===========================================
# SUPABASE DATABASE
# ===========================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/database

# Database connection (Transaction mode for Serverless)
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres

# Supabase project URL
SUPABASE_URL=https://[PROJECT_REF].supabase.co

# Supabase anon/public key
SUPABASE_ANON_KEY=your-anon-key

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Serverpod server mode
SERVERPOD_MODE=development

# Server port (default: 8080)
SERVER_PORT=8080

# ===========================================
# OPTIONAL SETTINGS
# ===========================================
# Logging level: debug, info, warning, error
LOG_LEVEL=info

# Maximum files to index in parallel
MAX_PARALLEL_INDEXING=5

# Embedding batch size
EMBEDDING_BATCH_SIZE=20
