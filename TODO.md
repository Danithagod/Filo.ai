    [x] - the indexing should be a summary rather than a full document content embedding
    [x] - figure out a way to index files more efficiently in this current setup
    [x] -we need a better way to represent the indexed files
    [x] - the state management of the indexing process, the loading of the files and the indexing process is not accurate,
    the indexing process should first make sure all the files are read only and if there are other file types they can just have their titles and type indexed 
    [x] - there should be a way to remove the indexed files or ignore some files from the indexing process
    [x] -we also wanna implement agentic features with chat window, that can perform organisation tasks such as renaming folders, shifting files to other folders, deleting files, etc all through structured output for the agent using terminal commands
    [x] - need a loading or landing page to show the user that the app is loading and for on-boarding and permissions for this 
    [x] - the indexing feature needs proper testing and implementation
    [x] - The index feature isnt working for some reason, the frontend isnt updating and more logs and thorogh testing is required with the backend
    [/] - need to make the searh window more like raycast
    [ ] - the Chat Screen should have a better looking UI
    [ ] - light and dark mode does not have smooth transition
    [ ] - Refactor the HomeScreen for better UI 
    [x] - Rename the app to Filo (Phase 1 Complete: UI & Display Names) 
    [x] - need to design and update the app icon for all platforms and first create a plan for this  
    [ ] -we need to fix the @ feature drawer
    [ ] - we can add special tags for files and folders to make it easier to search/refernce them
    [] -
    [] -
    [] -
    [] -
    [] -
    [] -